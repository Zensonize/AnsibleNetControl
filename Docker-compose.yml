version: "3.7"

services: 
    ansible:
        image: ubuntu:20.04
        working_dir: /ansible
        ports: 
            - 4000:4000
        volumes: 
            - ${PWD}/ansible:/ansible
            - ${PWD}/temp:/temp
        command: sh -c "chmod +x setup.sh && ./setup.sh"
